import {StyleSheet} from "react-native"

export default StyleSheet.create({
  pergunta: { 
    color: '#7500CF',
    fontSize: 24,
    textAlign: 'center',
    padding: 30,
    fontWeight: '500'
  }
});
